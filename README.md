# pyfiles
catch files as doc, pdf and xls of any web site
need you install in python the librarys as: requests, beautifulsoup4, wget.

# to execute the script

1. in one line call to Python, followed by space call to script(this python file), follwed by space and two params, 1 the link of web site and name of folder to save the files.
